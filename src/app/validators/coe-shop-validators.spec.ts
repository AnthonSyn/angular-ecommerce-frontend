import { CoeShopValidators } from './coe-shop-validators';

describe('CoeShopValidators', () => {
  it('should create an instance', () => {
    expect(new CoeShopValidators()).toBeTruthy();
  });
});
